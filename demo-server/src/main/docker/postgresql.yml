version: '2'
services:
    demo-postgresql:
        image: quangtrung89/postgres
        # volumes:
        #     - ~/volumes/jhipster/demo/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=demo
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
